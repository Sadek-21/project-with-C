#include <stdio.h>
#include <stdlib.h>

int main()
{



    /*int N , S , i ;
    do{
        printf("Donner un nomber positif ");
        scanf("%d",&N);
        if (N!=0){
        S=0;
        for{i=1;i<N;i++}
        if(N%i==0)
            S=S+i;
        if(S==N)
            printf("%d est parfait \n\n",N);
        else
            printf("%d n'est pas parfait",N );
        }
        else
        printf("au revoir");
    }while(N!=0)
    */


    /*int a , b , S , i ;
    printf("Veuilles enter un nomber :\n");
    scanf("%d"&a);
    printf("Veuilles enter un nomber :\n");
    scanf("%d"&b);
    S=0;
    for(i=1;i<=b;i++)
        S=S+a;
        printf("Resultat:%d*%d = %d",a,b,S);
        */

    /*int N ,i ;
    float F ;
    printf("Veuilles saisir la valeur de N :");
    scanf("%d",&N);
    F = 1;
    if (N>0){
       for (i=1;i<=N;i++){
        F=F*i;
       }
       printf("la factorielle de %d est :%.2f",N,F);
    }
    else if (N==0){
        printf("La factorielle est : 1");
    }
    else{
        printf("Veuilles sasir un nomber positiv");
    }
    */
    /*int N , S ,i ;
    printf("Veuilles saisir la valeur de N :");
    scanf("%d",&N);
    S=0;
    for (i=0;i<=N;i++)
         S=S+i;

    printf("La somme est :%d",S);*/
    return 0;
}
